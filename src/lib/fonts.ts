/**
 * Font family strings that reference the CSS variables injected by next/font.
 * Use these everywhere instead of hardcoded font name strings like "'Playfair Display', serif".
 * This guarantees the font is always the one loaded by Next.js at build time.
 */

export const fontPlayfair  = "var(--font-playfair), Georgia, serif";
export const fontCormorant = "var(--font-cormorant), Georgia, serif";
export const fontMontserrat = "var(--font-montserrat), system-ui, sans-serif";
export const fontScript    = "var(--font-script), cursive";
